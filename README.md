Commands:
___________
git add mostafa.py
git status
git commit -m "name"
git log --oneline
git commit -am "name+age"
git commit -am "name+age+job"
git commit -am "name+age+job+hobby"
git reset --hard HEAD~2
git commit -am "commit after deletion"
git commit --amend -m "finish"

git remote add origin git@github.com:MostafaMasoud288/project_a.git
git branch -m main
git push -u origin main

git revert HEAD~1
git push --force
